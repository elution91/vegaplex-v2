# Copy to .env.production and set the deployed backend URL.
# In Vercel, set this as an Environment Variable in the project settings instead
# of committing it.
VITE_API_URL=https://vegaplex-api.onrender.com
